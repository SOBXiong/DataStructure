#include <stdio.h>
#include <stdlib.h>
/**
 * @description: 输入: S={1,2,...,n}为n项活动的集合,
 *                    si,fi分别为活动i的开始和结束时间
 *                    活动i与j相容 <=> si >= fj 或 sj >= fi
 *               求: 最大的两两相容的活动集A 
 * 
 * @method: 结束早的优先,排序使f1 <= f2 <= ... <= fn,从前向后挑选
 **/
int main(){

    return 0;
}